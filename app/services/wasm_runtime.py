"""WASM plugin runner placeholder for priority logic extension."""


def run_wasm_priority_plugin(title: str, description: str) -> int | None:
    """Return None when no runtime/plugin is loaded; app uses default AI fallback."""
    return None
